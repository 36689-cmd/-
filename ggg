import tkinter as tk import time import threading def fake_hack(): texts = [ "กำลังเชื่อมต่อเซิร์ฟเวอร์...", "ดึงข้อมูลผู้ใช้...", "เจาะระบบสำเร็จ!", "กำลังอัปโหลดข้อมูล...", "เสร็จสิ้น!" ] for t in texts: label.config(text=t) time.sleep(1) label.config(text="โดนแกล้งแล้ว 😆") window = tk.Tk() window.title("SYSTEM") window.geometry("400x200") label = tk.Label(window, text="เริ่มต้นระบบ...", font=("Arial", 16)) label.pack(expand=True) threading.Thread(target=fake_hack).start() window.mainloop()
